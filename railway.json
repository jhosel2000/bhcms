{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "releaseCommand": "echo \"RELEASE PHASE STARTING\" && php artisan migrate --force --verbose && echo \"RELEASE PHASE ENDED\"",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  },
  "engines": {
    "php": "8.2"
  }
}
