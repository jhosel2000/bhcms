{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "releaseCommand": "php artisan migrate --force",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  },
  "engines": {
    "php": "8.2"
  }
}
